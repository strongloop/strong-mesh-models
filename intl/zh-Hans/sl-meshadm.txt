使用：{{%MAIN%}[选择余地〕[指挥…〕…管理{{Strongloop Mesh}。选择余地：  {{-h,--help}印刷对有帮助和出口。{{-v,--version}印刷版本和  出口。为的过程经理{{-C,--control CTL}控制手段终点。为的过程经理控制手段终点被为搜查寻找，若非有秩序这指定作为：1。在环境中的`STRONGLOOP_PM`：可以是一本地领土穿过的通道或者一{{HTTP URL}。2。`{{http://localhost:8701}`：一个以连着写{{localhost}的过程经理当使用一{{HTTP URL}的时候，它能随意地被把礼仪换成`{{http+ssh://}`在嘘的上方挖。{{ssh username}将对你的当前用户不履行义务和证明向使用你的当前{{ssh-agent}不履行义务。{{username}能被按设定一`{{SSH_USER}`环境变数不理会。证明能被使朝向私有的关键的穿过的通道被使用`{{SSH_KEY}`环境变数不理会使用一代替一个代理人的存在私有的关键。为嘘使用港口的向嘘按照向左转(22)但是能被不理会的按设定`{{SSH_PORT}`的标准不履行环境变数。执行者指挥：`{{EXEC}`是执行者身份证。它能被列出执行者为得到使用`{{exec-list}`。  {{exec-create}建立一个执行者。{{exec-remove EXEC}从开除  执行者 EXEC。{{exec-list}清单执行者。{{exec-shutdown EXEC}  停业执行者 EXEC。网关审计员指挥：`GW`是网关审计员身份证。它能被列出网关审计员为得到使用`{{gw-list}`。  {{gw-create}建立一个网关审计员。{{gw-remove GW}从开除网关审计员 GW。{{gw-list}清  单网关审计员。{{gw-shutdown GW}停业网关审计员 GW。
