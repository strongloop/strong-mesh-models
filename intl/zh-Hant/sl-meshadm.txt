使用：{{%MAIN%}[選擇餘地〕[指揮…〕…管理{{Strongloop Mesh}。選擇餘地：  {{-h,--help}印刷對有幫助和出口。{{-v,--version}印刷版本和  出口。為的過程經理{{-C,--control CTL}控制手段終點。為搜查為的過程經理控制手段終點尋找，如果不有秩序這指定作為：1。在環境中的`STRONGLOOP_PM`：可以是一本地領土穿過的通道或者一{{HTTP URL}。2。`{{http://localhost:8701}`：一個以連著寫{{localhost}的過程經理當使用一{{HTTP URL}的時候，它能隨意地透過把禮儀換成`{{http+ssh://}`在噓的上方被挖。{{ssh username}將對你的目前用戶不履行義務和證明向使用你的目前{{ssh-agent}不履行義務。{{username}能透過按設定一`{{SSH_USER}`環境變數被不理會。證明能透過使朝向私有的關鍵的穿過的通道被使用`{{SSH_KEY}`環境變數被不理會使用一代替一個代理人的存在私有的關鍵。為噓使用港口的向噓按照向左轉(22)但是能被不理會的按設定`{{SSH_PORT}`的標準不履行環境變數。執行者指揮：`{{EXEC}`是執行者愛達荷州。它能透過列出執行者被為得到使用`{{exec-list}`。  {{exec-create}建立一個執行者。{{exec-remove EXEC}強行帶  離執行者 EXEC。{{exec-list}清單執行者。  {{exec-shutdown EXEC}停業執行者 EXEC。閘道審計員指揮：`GW`是閘道審計員愛達荷州。它能透過列出閘道審計員被為得到使用`{{gw-list}`。  {{gw-create}建立一個閘道審計員。{{gw-remove GW}強行帶離閘道審計員 GW。{{gw-list}  清單閘道審計員。{{gw-shutdown GW}停業閘道審計員 GW。
